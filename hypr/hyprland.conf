################
### MONITORS ###
################

monitor=DP-5,1920x1080@280,0x0,1
monitor=HDMI-A-2,1920x1080@75,1920x0,1
workspace=1,monitor:DP-5
workspace=2,monitor:HDMI-A-2


# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = rofi -show drun
$ai = chatgpt-desktop
$stream = obs
$notes = notion-snap-reborn


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar
exec-once = hyprpaper


#############################
### ENVIRONMENT VARIABLES ###
#############################

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

general {
    gaps_in = 3
    gaps_out = 8

    border_size = 3

    col.active_border = rgba(FF69B4FF) rgba(59009BFF) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 10

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 3

        vibrancy = 0.1696
    }
}

animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

master {
    new_status = master
}

misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

input {
    kb_layout = us,jp
    
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# Example per-device config
device {
    name = mambasnake-m5-ultra-mouse-2.4g-consumer-control
    sensitivity = -1
}


###################
### KEYBINDINGS ###
###################

$mainMod = alt # Sets "Windows" key as main modifier

bind = $mainMod, M, exit,
bind = $mainMod SHIFT, W, exec, pkill waybar && waybar &,
bind = $mainMod SHIFT, P, exec, pkill hyprpaper && hyprpaper &,
bind = $mainMod SHIFT, C, exec, google-chrome &,
bind = $mainMod, B, exec, blueman-manager &,
bind = $mainMod SHIFT, B, exec, ~/Downloads/GalaxyBudsClient_Linux_64bit_Portable.bin &,
bind = $mainMod SHIFT, T, exec, gnome-clocks,
bind = $mainMod, N, exec, hyprsunset &,
bind = $mainMod, D, exec, discord &,
bind = $mainMod SHIFT, L, exec, ~/.local/bin/toggle-japanese,
bind = $mainMod, T, exec, tradingview --ozone-platform=x11 &,
bind = $mainMod SHIFT, S, exec, flatpak run com.spotify.Client &,
bind = $mainMod SHIFT, D, exec, hyprshot -m region --clipboard-only,
bind = CTRL SHIFT, D, exec, hyprshot -m window --clipboard-only,
bind = $mainMod, F, fullscreen, 
bind = $mainMod, S, togglesplit, 
bind = ,XF86AudioMute, exec, hyprlock &,
bind = $mainMod, C, forcekillactive,
bind = $mainMod SHIFT, N, exec, $notes
bind = Ctrl, Space, exec, $menu
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, A, exec, $ai
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, O, exec, $stream
bind = $mainMod, Space, exec, $menu

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with F1-F10
bind = ,F1, workspace, 1
bind = ,F2, workspace, 2
bind = ,F3, workspace, 3
bind = ,F4, workspace, 4
bind = ,F5, workspace, 5
bind = ,F6, workspace, 6
bind = ,F7, workspace, 7
bind = ,F8, workspace, 8
bind = ,F9, workspace, 9
bind = ,F10, workspace, 10

# move workspaces with F1-F10
bind = $mainMod,F1, movetoworkspace, 1
bind = $mainMod,F2, movetoworkspace, 2
bind = $mainMod,F3, movetoworkspace, 3
bind = $mainMod,F4, movetoworkspace, 4
bind = $mainMod,F5, movetoworkspace, 5
bind = $mainMod,F6, movetoworkspace, 6
bind = $mainMod,F7, movetoworkspace, 7
bind = $mainMod,F8, movetoworkspace, 8
bind = $mainMod,F9, movetoworkspace, 9
bind = $mainMod,F10, movetoworkspace, 10

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10
bind = ,XF86AudioMute, exec, hyprlock,

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%-
# bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

bind = ALT, TAB, submap, mousemode

submap = mousemode

# Normal movement
bind = , h, exec, kbd-mouse.sh left
bind = , l, exec, kbd-mouse.sh right
bind = , k, exec, kbd-mouse.sh up
bind = , j, exec, kbd-mouse.sh down

# Faster movement (Shift)
bind = SHIFT, h, exec, kbd-mouse.sh fast-left
bind = SHIFT, l, exec, kbd-mouse.sh fast-right
bind = SHIFT, k, exec, kbd-mouse.sh fast-up
bind = SHIFT, j, exec, kbd-mouse.sh fast-down

# Scroll mode (Super)
bind = SUPER, k, exec, kbd-mouse.sh scroll-up
bind = SUPER, j, exec, kbd-mouse.sh scroll-down
bind = SUPER, h, exec, kbd-mouse.sh scroll-left
bind = SUPER, l, exec, kbd-mouse.sh scroll-right

# Clicks
bind = , RETURN, exec, kbd-mouse.sh left-click
bind = , g, exec, kbd-mouse.sh right-click

# Exit nav mode
bind = ALT, TAB, submap, reset
bind = , ESCAPE, submap, reset

submap = reset
